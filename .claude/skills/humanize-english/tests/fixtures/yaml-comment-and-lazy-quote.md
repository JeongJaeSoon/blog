---
lang: en
title: Round seventeen controls # it's only a working title
summary: A YAML comment never renders, and a quote runs on without its marker.
---

> The quoted line keeps its marker and it doesn't count.
The next line drops the marker and it wasn't counted either, because a
blockquote continues lazily.

The quote ends at the blank line above, so this sentence isn't quoted and it
counts.

> Another quote, which ends at a heading.

## A heading is a new block

The heading above ended the quote, so this line isn't part of it.
