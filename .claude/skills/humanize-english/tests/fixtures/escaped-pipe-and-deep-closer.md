---
title: Round fifteen controls
summary: An even backslash run before a pipe, and a closer six spaces in.
lang: en
---

| a `foo \\| can't edit ` | other |
|---|---|
| `bar \| kept` | it wasn't split |

A top-level fence opened three spaces in cannot be closed at six:

   ```text
   error: can't open
      ```
   we haven't left the block yet
   ```

Back at the margin, nothing here contracts.
